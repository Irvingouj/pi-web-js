# Archived planning documents

Temporary planning docs that drove the `problems.md` → 0.10.0 work. Kept for
history; no longer authoritative. Shipped behavior lives in `CHANGELOG.md` and
the test suite (`web/tests/e2e/extension/problems-*.spec.ts`).

- `problems.md` — product requirements (P0–P2) for extension-js observation /
  targeting / binary / cold-tab / docs.
- `PROBLEMS-TDD-PLAN.md` — WU-0–WU-10 TDD plan mapping requirements to tests.
- `GOAL_STATEMENT.md` — execution driver for the plan above.
- `MANIFEST-DOCS-STRICT-TYPING-PLAN.md` — WU-1–WU-5 strict manifest typing plan.
- `BROWSERGENT_DEFERRED.md` — consumer-side (Browsergent) follow-ups, out of
  scope for extension-js packaging.
